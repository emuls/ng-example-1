# NG Example 1

Basig NG examples

### Setup

* Clone repo
* ```npm install```
* ```grunt build```
* ```cd server```
* ```node server.js```
* Open a browser and go to [http://localhost:3000](http://localhost:3000)